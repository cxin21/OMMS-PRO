/**
 * Embedding Module
 */

export { EmbeddingService } from './embedding-service';
// Re-export EmbeddingConfig from types/config for convenience
export type { EmbeddingConfig } from '@core/types/config';
